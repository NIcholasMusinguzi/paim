services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: paim
      POSTGRES_USER: paim
      POSTGRES_PASSWORD: paim
    ports: ["5432:5432"]
    volumes: ["paim-db:/var/lib/postgresql/data"]
  redis:
    image: redis:7
    ports: ["6379:6379"]
volumes:
  paim-db:
